services:
  - type: web
    name: my-web-app
    runtime: python-3.12.10   # Required for Python version
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py  # Replace with your app entrypoint