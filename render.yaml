services:
  - type: web
    name: emotion-detection-app
    runtime: python-3.12.10  # Python version
    plan: free               # or starter/pro depending on your plan
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # replace with your entrypoint